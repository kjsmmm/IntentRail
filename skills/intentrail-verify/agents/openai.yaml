interface:
  display_name: "IntentRail Verify"
  short_description: "Verify results against the latest active intent"
  default_prompt: "Use $intentrail-verify to check work against current intent."

policy:
  allow_implicit_invocation: false
